import React from "react";

function ChirpJumbotron({ }) {
    return (
        <div
            style={{ height: 500, clear: "both", paddingTop: 120, textAlign: "center" }}
            className="jumbotron">
        </div>
    );
}

export default ChirpJumbotron;